{
  "name": "classifier-addon",
  "version": "1.0.0",
  "description": "Minimal bare addon example: binary classification with logistic regression",
  "addon": true,
  "scripts": {
    "build": "bare-make generate && bare-make build && bare-make install",
    "example": "bare example.js",
    "test": "bare test/test-classifier.js"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.12.0",
    "cmake-bare": "1.7.5",
    "cmake-vcpkg": "^1.1.0"
  },
  "engines": {
    "bare": ">=1.24.0"
  }
}
